import {Feature} from './feature';

class SignUp implements Feature{
    constructor(params){
        let bot = params.bot;
        let mongoDBConnection = params.mongoDBConnection;
    }
    init: () => {
        
    };
}